apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "pe-agent.fullname" . }}-api
  labels:
    {{- include "pe-agent.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "pe-agent.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
