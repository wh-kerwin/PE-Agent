apiVersion: v1
kind: Service
metadata:
  name: {{ include "pe-agent.fullname" . }}
  labels:
    {{- include "pe-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "pe-agent.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
